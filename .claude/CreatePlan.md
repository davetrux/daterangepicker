The overall goal is to convert this project to be an Angular
component library. As a front-end developer, make a plan to do that.

Use the following requirements:

* Convert the project to an Angular v21 component
  * Use CSS
  * No server-side rendering
  * Angular cli is installed globally
* Keep the boostrap, bootstrap-icons, and dayjs libraries
* Convert JavaScript to Typescript
* Remove the use of JQuery in the code, converting JQuery patterns and usage into Angular patterns.
* Remove JQuery from the project completely.
* Ignore the example directory
* Ignore the website directory
* Remove bower, replace any necessary functionality with npm-based command and the angular CLI
* Update CLAUDE.md
